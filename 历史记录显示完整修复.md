# 历史记录显示完整修复

## 🔄 修复内容

根据用户反馈，**撤销了去重逻辑**，保持历史记录的完整性。

## ✅ 修改

### 文件: `app/api/user/generated-content/route.ts`

#### 修改前（有去重）
```typescript
// 🆕 去重逻辑：如果 initialPrompt 相同且创建时间接近（5分钟内），只保留最新的一条
const deduplicatedContents = contents.reduce((acc, current) => {
  const existingIndex = acc.findIndex(item => {
    if (!item.initialPrompt || !current.initialPrompt) return false
    const isSamePrompt = item.initialPrompt.trim() === current.initialPrompt.trim()
    const timeDiff = Math.abs(new Date(item.createdAt).getTime() - new Date(current.createdAt).getTime())
    const isNearTime = timeDiff < 5 * 60 * 1000 // 5分钟内
    return isSamePrompt && isNearTime
  })
  
  if (existingIndex === -1) {
    acc.push(current)
  } else {
    // 如果找到相同的，保留较新的那个
    const existingTime = new Date(acc[existingIndex].createdAt).getTime()
    const currentTime = new Date(current.createdAt).getTime()
    if (currentTime > existingTime) {
      acc[existingIndex] = current
    }
  }
  return acc
}, [] as typeof contents)

return NextResponse.json({
  success: true,
  contents: deduplicatedContents.map(c => ({ /* ... */ }))
})
```

#### 修改后（无去重，完整返回）
```typescript
console.log(`✅ [CONTENT-API] 获取用户内容列表: ${contents.length}条`)

return NextResponse.json({
  success: true,
  contents: contents.map(c => ({
    ...c,
    questions: c.questions ? JSON.parse(c.questions) : [],
    answers: c.answers ? JSON.parse(c.answers) : [],
    tags: c.tags ? JSON.parse(c.tags) : [],
    createdAt: c.createdAt.toISOString(),
    updatedAt: c.updatedAt.toISOString()
  })),
  total,
  limit,
  offset
})
```

## 📊 效果

### 现在的行为
历史记录会**完整显示**所有数据库中的记录，包括：
- ✅ 相同 prompt 的多次生成
- ✅ 所有时间的记录
- ✅ 按创建时间倒序排列（最新的在前）

### 示例
如果用户多次生成相同的内容，历史记录会显示所有次数：

```
故事记录:
- 我觉得自己像个反社会女反派 (0 张图片) 2025/10/31 13:30
- 我觉得自己像个反社会女反派 (0 张图片) 2025/10/31 13:15
- 我觉得自己像个反社会女反派 (3 张图片) 2025/10/31 13:00
- 我觉得是个反社会女反派 像汉尼拔 (3 张图片) 2025/10/30 18:00
- 我觉得是个反社会女反派 像汉尼拔 (3 张图片) 2025/10/30 17:45
- 我就想 我要是没出国留学 (2 张图片) 2025/10/30 10:00
...（所有记录都会显示）
```

## 🎯 保留的优化

虽然移除了去重逻辑，但保留了以下优化：

1. ✅ **移除重复的 useEffect** (`components/ChatHistorySidebar.tsx`)
   - 避免数据被加载 2 次
   - 提高性能

2. ✅ **完整的数据返回**
   - 所有字段正确解析（questions, answers, tags）
   - 时间格式标准化（ISO 格式）

## 📝 修改的文件

1. ✅ `app/api/user/generated-content/route.ts` - 移除去重逻辑
2. ✅ `components/ChatHistorySidebar.tsx` - 保持单次加载（之前已修复）
3. ✅ `历史记录显示完整修复.md` - 本文档

---

**更新时间**: 2025-10-31  
**状态**: ✅ 已修复 - 历史记录完整显示，不去重


